{
  "swagger": "2.0",
  "info": {
    "title": "mesh.y.proto",
    "version": "0.0.1"
  },
  "tags": [
    {
      "name": "PrivateTransferTransport"
    }
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/v1/interconn/chan/push": {
      "post": {
        "summary": " 容器调用通信模块发送数据",
        "operationId": "push",
        "responses": {
          "200": {
            "description": "A successful response."
          }
        },
        "tags": [
          "PrivateTransferTransport"
        ]
      }
    },
    "/v1/interconn/chan/pop": {
      "post": {
        "summary": "容器调用通信模块接收数据",
        "operationId": "pop",
        "responses": {
          "200": {
            "description": "A successful response."
          },
          "default": {
            "description": "An unexpected error response."
          }
        },
        "tags": [
          "PrivateTransferTransport"
        ]
      }
    },
    "/v1/interconn/chan/peek": {
      "post": {
        "summary": "容器调用通信模块快速获取数据",
        "operationId": "peek",
        "responses": {
          "200": {
            "description": "A successful response."
          },
          "default": {
            "description": "An unexpected error response."
          }
        },
        "tags": [
          "PrivateTransferTransport"
        ]
      }
    },
    "/v1/interconn/chan/release": {
      "post": {
        "summary": "容器调用通信模块会话释放",
        "operationId": "release",
        "responses": {
          "200": {
            "description": "A successful response."
          },
          "default": {
            "description": "An unexpected error response."
          }
        },
        "tags": [
          "PrivateTransferTransport"
        ]
      }
    }
  }
}
